Fixture category: prompt templates

This directory contains executable mini workspaces for P1-M8 prompt-template discovery
and slash-style expansion. Each child directory owns its own `.pi/prompts/` tree and is
covered by runtime resource tests.
