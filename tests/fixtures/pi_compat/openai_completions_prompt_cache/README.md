# Fixture: `openai_completions_prompt_cache`

- Status: M2 provider-cache fixture.
- Purpose: OpenAI Chat Completions direct cache fields and compatible-provider
  session-affinity headers are verified offline.

