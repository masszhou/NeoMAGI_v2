Fixture category: skills

This directory contains executable mini workspaces for P1-M8 skill discovery and
`/skill:<name>` expansion. Each child directory owns its own `.magipi/skills/` tree and
is covered by runtime resource tests.
