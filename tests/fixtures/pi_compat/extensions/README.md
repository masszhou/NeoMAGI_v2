Fixture category: extensions

This directory contains executable mini workspaces for P1-M8 extension runtime behavior.
Each child directory owns its own `.magipi/extensions/` tree and is covered by entrance-level
runtime tests rather than by JSON round-trip tests.
