---
description: Continue the QMD autoresearch mini loop from persisted artifacts
argument-hint: "[focus]"
---
Use the `autoresearch-mini` skill. Read `autoresearch.md`, the tail of `autoresearch.jsonl`, and `git log --oneline -5`, then propose and run exactly one next hypothesis.

Optional focus: $ARGUMENTS
